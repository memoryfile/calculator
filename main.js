// let input = a;
// let input2 = b;
let z = 0;
const inputtedNumbers = [x, y];

function add(x, y) {
  let sum = x + y
  return sum;
}

function subtract(x, y) {
  let difference = x - y
  return difference;
}

function multiply(x, y) {
  let product = x * y
  return product;
}

function divide(x, y) {
  let quotient = x / y
  return quotient;
}

function createCalculatorButtons() {
  while (z > 10) {
    z += 1;
  }
}
